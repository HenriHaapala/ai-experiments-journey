name: Deploy to Oracle Cloud

on:
  workflow_dispatch:  # Manual deployment only
  workflow_run:       # Auto-deploy after CI passes
    workflows: ["CI/CD Pipeline"]
    types: [completed]
    branches: [main]

jobs:
  # Check if CI passed
  check-ci:
    name: Check CI Status
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    outputs:
      should-deploy: ${{ steps.check.outputs.result }}
    steps:
      - name: Check CI conclusion
        id: check
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            echo "result=true" >> $GITHUB_OUTPUT
            echo "✅ Manual deployment triggered"
          elif [ "${{ github.event.workflow_run.conclusion }}" == "success" ]; then
            echo "result=true" >> $GITHUB_OUTPUT
            echo "✅ CI passed, proceeding with deployment"
          else
            echo "result=false" >> $GITHUB_OUTPUT
            echo "❌ CI did not pass, skipping deployment"
          fi

  # Deploy to Oracle Cloud
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: check-ci
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up SSH key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
          SSH_HOST: ${{ secrets.OCI_HOST }}
          SSH_USER: ${{ secrets.OCI_USER }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts
          echo "✅ SSH key configured"

      - name: Test SSH connection
        env:
          SSH_HOST: ${{ secrets.OCI_HOST }}
          SSH_USER: ${{ secrets.OCI_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST 'echo "✅ SSH connection successful"'

      - name: Deploy to Oracle Cloud
        env:
          SSH_HOST: ${{ secrets.OCI_HOST }}
          SSH_USER: ${{ secrets.OCI_USER }}
        run: |
          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST << 'ENDSSH'
            set -e  # Exit on critical errors only (we handle non-critical ones with ||)
            set -x  # Print each command for debugging

            echo "🚀 Starting deployment..."

            # Navigate to project directory
            cd ~/ai-portfolio

            # Pull latest code
            echo "📥 Pulling latest code from GitHub..."
            git fetch origin
            git reset --hard origin/main

            # Ensure NEXT_PUBLIC_API_BASE_URL is set in .env for production
            if ! grep -q "NEXT_PUBLIC_API_BASE_URL" .env; then
              echo "➕ Adding NEXT_PUBLIC_API_BASE_URL to .env..."
              echo "NEXT_PUBLIC_API_BASE_URL=https://wwwportfolio.henrihaapala.com" >> .env
            fi

            # Stop and remove old containers first
            echo "🛑 Stopping and removing old containers..."
            docker-compose down --remove-orphans

            # Force remove any stuck containers
            echo "🧹 Cleaning up stuck containers..."
            (docker ps -a | grep aiportfolio || true) | awk '{print $1}' | xargs -r docker rm -f || true

            # NUCLEAR OPTION: Remove ALL Docker cache to force complete rebuild
            echo "💥 Removing ALL Docker build cache..."
            docker builder prune -af
            docker system prune -af --volumes

            # Pull latest Docker images
            echo "📦 Pulling Docker images..."
            docker-compose pull

            # Rebuild services with no cache AND pull base images
            echo "🔨 Rebuilding Docker images (no cache, forced)..."
            docker-compose build --no-cache --pull

            # Start new containers with force recreate
            echo "🚀 Starting new containers (force recreate)..."
            docker-compose up -d --force-recreate

            # Wait for postgres
            echo "⏳ Waiting for PostgreSQL..."
            sleep 10

            # Install pgvector extension
            echo "🔧 Installing pgvector extension..."
            docker-compose exec -T postgres psql -U aiportfolio aiportfolio -c "CREATE EXTENSION IF NOT EXISTS vector;"

            # Wait for backend
            echo "⏳ Waiting for backend..."
            sleep 10

            # Run database migrations
            echo "🗃️ Running database migrations..."
            docker-compose exec -T backend python manage.py migrate --noinput

            # Collect static files (if needed)
            echo "📦 Collecting static files..."
            docker-compose exec -T backend python manage.py collectstatic --noinput || echo "⚠️ Static files warning (might be expected)"

            # Verify all services are running
            echo "✅ Checking service health..."
            docker-compose ps

            # Test backend health
            echo "🔍 Testing backend health endpoint..."
            curl -f http://localhost:8000/api/health/ || echo "⚠️ Backend health check failed"

            # Test frontend
            echo "🔍 Testing frontend..."
            curl -f http://localhost:3000 || echo "⚠️ Frontend health check failed"

            # Flush Redis (clear agent conversation cache)
            echo "🗑️ Flushing Redis cache..."
            docker-compose exec -T redis redis-cli FLUSHALL || echo "⚠️ Redis flush failed (non-critical)"

            # Reload nginx and clear cache
            echo "🔄 Reloading nginx and clearing cache..."
            sudo rm -rf /var/cache/nginx/* || echo "No nginx cache to clear"
            sudo nginx -t && sudo systemctl reload nginx

            # Verify frontend build changed
            echo "🔍 Verifying frontend build..."
            BUILD_ID=$(docker-compose exec -T frontend cat /app/.next/BUILD_ID 2>/dev/null || echo "UNKNOWN")
            echo "   Next.js Build ID: $BUILD_ID"

            # Test cache headers
            echo "🔍 Testing cache headers..."
            curl -sI http://localhost:3000 | grep -i "cache-control" || echo "⚠️ No cache-control header from frontend container"
            curl -skI https://wwwportfolio.henrihaapala.com | grep -i "cache-control" || echo "⚠️ No cache-control header from nginx"

            echo "🎉 Deployment completed successfully!"
            echo "🌐 Site: https://wwwportfolio.henrihaapala.com"
            echo "📦 Build ID: $BUILD_ID"

            # Show container status
            echo ""
            echo "📊 Container Status:"
            docker-compose ps

          ENDSSH

      - name: Post-deployment verification
        env:
          SITE_URL: https://wwwportfolio.henrihaapala.com
        run: |
          echo "🔍 Verifying deployment..."

          # Wait for service with timeout and retries (production-grade health check)
          wait_for_service() {
            local url=$1
            local name=$2
            local timeout=300  # 5 minutes total timeout
            local interval=10  # Check every 10 seconds
            local elapsed=0

            echo "⏳ Waiting for $name to become healthy..."

            while [ $elapsed -lt $timeout ]; do
              status=$(curl -f -s -o /dev/null -w "%{http_code}" "$url" 2>/dev/null || echo "000")

              if echo "$status" | grep -q "200\|301\|302"; then
                echo "✅ $name is healthy (HTTP $status) - took ${elapsed}s"
                return 0
              fi

              echo "   ⏳ $name not ready yet (HTTP $status) - waiting... (${elapsed}/${timeout}s)"
              sleep $interval
              elapsed=$((elapsed + interval))
            done

            echo "❌ $name failed to become healthy after ${timeout}s (last status: HTTP $status)"
            return 1
          }

          # Check all services (fail fast if any service fails)
          wait_for_service "$SITE_URL" "Frontend" || exit 1
          wait_for_service "$SITE_URL/api/health/" "Backend API" || exit 1
          wait_for_service "$SITE_URL/agent/health" "Agent Service" || exit 1

          echo ""
          echo "🎉 All services are healthy and responding!"
          echo "🌐 Site: $SITE_URL"

      - name: Cleanup SSH key
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
          echo "🧹 SSH key cleaned up"

      - name: Send deployment notification
        if: success()
        run: |
          echo "🚀 Deployment successful!"
          echo "Site: https://wwwportfolio.henrihaapala.com"
          echo "Commit: ${{ github.sha }}"
          echo "Deployed by: ${{ github.actor }}"

      - name: Rollback on failure
        if: failure()
        env:
          SSH_HOST: ${{ secrets.OCI_HOST }}
          SSH_USER: ${{ secrets.OCI_USER }}
        run: |
          echo "❌ Deployment failed, attempting rollback..."

          # Re-setup SSH key for rollback
          echo "${{ secrets.OCI_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST << 'ENDSSH'
            cd ~/ai-portfolio

            # Restore previous version
            git checkout HEAD~1
            docker-compose down
            docker-compose up -d

            echo "🔄 Rolled back to previous version"
          ENDSSH

  # Job to notify on deployment status
  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: deploy
    if: always()

    steps:
      - name: Deployment Status
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "✅ Deployment succeeded!"
            echo "🌐 Site: https://wwwportfolio.henrihaapala.com"
          else
            echo "❌ Deployment failed!"
            echo "Check logs for details"
            exit 1
          fi
