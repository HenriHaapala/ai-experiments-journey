# Production Server Configuration Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env.production
# 2. Fill in your actual server details
# 3. NEVER commit .env.production to git (it's already in .gitignore)

# Oracle Cloud Instance
OCI_HOST=your.server.ip.address
OCI_USER=ubuntu
OCI_SSH_KEY_PATH=/path/to/your/ssh-key.key

# Domain
PRODUCTION_DOMAIN=your-domain.com

# Usage Example:
# After setting OCI_HOST, you can use it like:
# ssh -i ${OCI_SSH_KEY_PATH} ${OCI_USER}@${OCI_HOST}
#
# Or source this file in your terminal:
# source .env.production
# ssh -i $OCI_SSH_KEY_PATH $OCI_USER@$OCI_HOST
