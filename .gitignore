############################
# Global OS / Editor junk
############################
.DS_Store
Thumbs.db
desktop.ini

.vscode/
.idea/
*.iml

############################
# Root-level environment / secrets
############################
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
*.pfx

# Keep .env.example (template) but ignore actual .env
!.env.example

############################
# Backend (Django / Python)
############################
backend/__pycache__/
backend/**/__pycache__/
backend/**/*.py[cod]
backend/.pytest_cache/
backend/.mypy_cache/
backend/.ruff_cache/

# Virtual env
backend/venv/

# Django stuff
backend/db.sqlite3
backend/staticfiles/
backend/media/

# Backend env file (VERY IMPORTANT)
backend/.env

############################
# Frontend (Next.js / Node)
############################
frontend/node_modules/
frontend/.next/
frontend/.turbo/
frontend/.cache/
frontend/out/

# Root node_modules (for Lefthook, Biome, etc.)
node_modules/
package-lock.json

# Frontend env files (VERY IMPORTANT)
frontend/.env
frontend/.env.*.local
frontend/.env.local
frontend/.env.development.local
frontend/.env.production.local
frontend/.env.test.local

# Lockfiles are OK to commit; if you prefer only one, ignore the other.
# (Pick one: package-lock or yarn.lock; you have package-lock so we keep it.)
# yarn.lock

############################
# Logs & temp files
############################
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

############################
# Docker / DB data (if you add later)
############################
data/
pgdata/
docker-data/

# Database backups (may contain sensitive data)
*.sql
database_backup.sql

# Migration scripts (temporary, project-specific)
migrate-database.sh

# .env.docker is a duplicate of .env.example - keep only .env.example
.env.docker

############################
# Security Scan Reports
############################
gitleaks-report.json
bandit-report.json
safety-report.json
semgrep-report.json
audit-report.json
*-report.json
*-report.html
coverage/
htmlcov/
.coverage
*.cover
